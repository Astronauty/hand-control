\subsection{Comparison against analytic grasp synthesis}
\label{sec:frogger}

\subsubsection{The min-weight metric, and how our formulation differs}
\label{sec:minweight}

Both our planner and FRoGGeR~\cite{li2023frogger} score a grasp by the
\emph{min-weight metric}. Let $W(q) \in \mathbb{R}^{6 \times m}$ be the primitive
wrench matrix at configuration $q$, each column the object-frame wrench
$[\boldsymbol{\tau}; \mathbf{f}]$ produced by a unit normal force along one generator
of one contact's linearized friction cone, so that $m = n_c s$ for $n_c$ contacts and
an $s$-sided cone. The metric is the value of
%
\begin{equation}
\ell^*(q) \;=\; \max_{\boldsymbol{\alpha},\,\beta} \; \beta
\quad \text{s.t.} \quad
W(q)\boldsymbol{\alpha} = \mathbf{0}, \;\;
\mathbf{1}^\top\boldsymbol{\alpha} = 1, \;\;
\boldsymbol{\alpha} \ge \beta\mathbf{1},
\label{eq:minweight}
\end{equation}
%
with the normalized form $\bar{\ell}^* = m\,\beta$ comparable across cone sizes.
Here $\boldsymbol{\alpha}$ is a convex combination of primitive wrenches that cancels
to zero net wrench and $\beta$ is its smallest weight, so $\beta > 0$ holds exactly
when the origin lies interior to the wrench hull---force closure---and its magnitude
measures how robustly. Crucially $\boldsymbol{\alpha}$ is \emph{not} constrained
non-negative: out of closure the optimum has $\beta < 0$ with some
$\alpha_j < 0$, which is the correct value and keeps \eqref{eq:minweight} smoothly
climbable rather than infeasible. This is what makes $\ell^*$ a usable objective
where the Ferrari--Canny $\epsilon$ metric, whose gradient vanishes at closure, is
not.

The two methods use \eqref{eq:minweight} differently, and the difference is the
subject of this comparison. FRoGGeR solves it as an inner problem to optimality at
every iterate and maximizes $\ell^*(q)$ as its \emph{sole} objective, subject to a
hard floor $\bar{\ell}^* \ge k_\ell$, with $q$ the only decision variable and
contacts pinned to the surface by an equality on the forward kinematics. We instead
\emph{embed} \eqref{eq:minweight}: $\boldsymbol{\alpha}$ and $\beta$ enter the
nonlinear program as ordinary decision variables alongside $q$ and a two-degree-of-freedom
surface coordinate per contact, with $W\boldsymbol{\alpha} = \mathbf{0}$ and
$\mathbf{1}^\top\boldsymbol{\alpha} = 1$ as constraints, and $-\beta$ enters a
weighted objective beside inverse-kinematics, regularization, and wrench-cone terms.
The solver's KKT system then couples $\beta$ to $q$ and the contact coordinates
directly, giving exact gradients without differentiating through a nested solve.

The embedding buys robustness to initialization and costs metric convergence, and we
report both. Because $\beta$ is one weighted term among several rather than the whole
objective---and is outvoted at the margin, the inverse-kinematics term carrying the
dominant placement gradient---$\beta$ equals the min-weight value only at
convergence of the whole program. Re-solving \eqref{eq:minweight} exactly on the same
$W$ at our returned iterate leaves a median optimality gap of $7.4 \times 10^{-3}$,
nonzero on every solve measured, where FRoGGeR's inner problem closes it exactly by
construction. Conversely, opposition between contacts is expressed in our objective
and need not be supplied by the seed: FRoGGeR's \eqref{eq:minweight}-only objective
contains no such preference, which is why its sampler pre-opens the fingers to the
object's bounding-box width and why the authors report performance to be
``highly sensitive to the sampled initial conditions.'' Our planner runs from a fixed
home configuration with no sampler.

\subsubsection{Protocol}

Both planners run in one simulator and share the scene, seeding pool, collision
model, execution stack, and scoring instruments; only the planner differs. Five YCB
objects, 20 seeds per object per method, 200 executed trials, scored by FRoGGeR's
shaky-pickup protocol: lift \SI{10}{\centi\meter} in \SI{1}{\second}, hold
\SI{1.5}{\second}, with \SI{3}{\milli\meter} sinusoidal perturbation in all spatial
axes, failing on $>30^\circ$ rotation or $>\SI{7.5}{\centi\meter}$ deviation from the
commanded trajectory. The baseline runs its own resample-until-feasible loop (20
attempts within \SI{60}{\second}) seeded by its bounding-box sampler; ours takes a
single solve. We simulate at $\mu = 2.0$, so $\bar{\ell}^*$ is comparable between
methods on this scene but not to absolute values in~\cite{li2023frogger}.

\begin{table}[t]
\centering
\caption{Execution, per object (20 seeds per object per method). \emph{Lift} is the
fraction of grasps reaching the lift phase; \emph{pick} applies the shaky-pickup
criteria of Li et al.; \emph{held} additionally requires every fingertip to still
carry load at the end of the lift, which displacement-only criteria cannot detect.
Brackets are $95\%$ Wilson intervals.}
\label{tab:execution}
\small
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}llcc@{}}
\toprule
object & metric & \textbf{Ours} & FRoGGeR \\
\midrule
\multirow{4}{*}{\texttt{lemon}} & lift $\uparrow$ & 100\% [84, 100] & 80\% [58, 92] \\
 & pick $\uparrow$ & 100\% [84, 100] & 40\% [22, 61] \\
 & held $\uparrow$ & 100\% [84, 100] & 35\% [18, 57] \\
 & $\bar{\ell}^*$ $\uparrow$ & 0.71 (0.71, 0.72) & 0.45 (0.35, 0.62) \\
\midrule
\multirow{4}{*}{\texttt{orange}} & lift $\uparrow$ & 100\% [84, 100] & 95\% [76, 99] \\
 & pick $\uparrow$ & 100\% [84, 100] & 70\% [48, 85] \\
 & held $\uparrow$ & 100\% [84, 100] & 55\% [34, 74] \\
 & $\bar{\ell}^*$ $\uparrow$ & 0.93 (0.90, 0.94) & 0.91 (0.84, 0.97) \\
\midrule
\multirow{4}{*}{\texttt{wood\_block}} & lift $\uparrow$ & 100\% [84, 100] & 60\% [39, 78] \\
 & pick $\uparrow$ & 25\% [11, 47] & 10\% [3, 30] \\
 & held $\uparrow$ & 10\% [3, 30] & 5\% [1, 24] \\
 & $\bar{\ell}^*$ $\uparrow$ & 0.73 (0.36, 0.92) & 0.37 (0.30, 0.44) \\
\midrule
\multirow{4}{*}{\texttt{tennis\_ball}} & lift $\uparrow$ & 100\% [84, 100] & 70\% [48, 85] \\
 & pick $\uparrow$ & 100\% [84, 100] & 20\% [8, 42] \\
 & held $\uparrow$ & 100\% [84, 100] & 20\% [8, 42] \\
 & $\bar{\ell}^*$ $\uparrow$ & 0.94 (0.92, 0.94) & 0.63 (0.49, 0.83) \\
\midrule
\multirow{4}{*}{\texttt{foam\_brick}} & lift $\uparrow$ & 100\% [84, 100] & 70\% [48, 85] \\
 & pick $\uparrow$ & 100\% [84, 100] & 35\% [18, 57] \\
 & held $\uparrow$ & 100\% [84, 100] & 30\% [15, 52] \\
 & $\bar{\ell}^*$ $\uparrow$ & 0.81 (0.55, 0.89) & 0.35 (0.30, 0.67) \\
\midrule
\multirow{4}{*}{\textbf{all}} & lift $\uparrow$ & \textbf{100\% [96, 100]} & 75\% [66, 82] \\
 & pick $\uparrow$ & \textbf{85\% [77, 91]} & 35\% [26, 45] \\
 & held $\uparrow$ & \textbf{82\% [73, 88]} & 29\% [21, 39] \\
 & $\bar{\ell}^*$ $\uparrow$ & \textbf{0.87 (0.71, 0.94)} & 0.52 (0.34, 0.83) \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Execution}

Table~\ref{tab:execution} reports per-object outcomes. Aggregated, our method
reaches the lift on $100\%$ $[96,100]$ of trials against $75\%$ $[66,82]$, succeeds
on $85\%$ $[77,91]$ against $35\%$ $[26,45]$, and holds on $82\%$ $[73,88]$ against
$29\%$ $[21,39]$; all three separate without overlap.

The deficit has two distinct sources: 25 of 100 baseline trials never reach the
lift, their solves placing contacts a median of \SI{17.5}{\milli\meter} from the
fingertip surface, and a further 40 grip the object and then lose it. Conditioned on
reaching the lift the baseline succeeds on $47\%$ against our $85\%$, so a single
rate conflates a reachability failure with a grasp-quality one. The modes differ in
kind---ours rotation-dominated (12 against 3), the baseline's deviation-dominated
(28 against 12), the object not held at all. Both methods fail on
\texttt{036\_wood\_block}, which demands $\gamma \approx \SI{26}{\newton}$ of
internal force against \SIrange{1}{2}{\newton} elsewhere and drives baseline solves
to the \SI{60}{\newton} stability ceiling.

\begin{table}[t]
\centering
\caption{Edge-seeking. For each solved contact we measure the geodesic distance along
the object surface to the nearest point where the surface normal turns by more than
$30^\circ$, a representation-independent edge test applied identically to both
methods. Margins are median (IQR) over contacts; \emph{near} is the fraction within
\SI{2}{\milli\meter} of an edge, with $95\%$ Wilson intervals.}
\label{tab:edge}
\small
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}llcc@{}}
\toprule
object & metric & \textbf{Ours} & FRoGGeR \\
\midrule
\multirow{2}{*}{\texttt{lemon}} & margin (mm) $\uparrow$ & 13.0 (12.0, 14.0) & 1.0 (1.0, 1.0) \\
 & near $\downarrow$ & 0\% [0, 39] & 100\% [61, 100] \\
\midrule
\multirow{2}{*}{\texttt{orange}} & margin (mm) $\uparrow$ & 18.5 (18.0, 19.0) & 17.5 (5.0, 18.8) \\
 & near $\downarrow$ & 0\% [0, 39] & 33\% [10, 70] \\
\midrule
\multirow{2}{*}{\texttt{wood\_block}} & margin (mm) $\uparrow$ & 12.0 (11.0, 13.0) & 1.0 (1.0, 1.0) \\
 & near $\downarrow$ & 0\% [0, 39] & 100\% [61, 100] \\
\midrule
\multirow{2}{*}{\texttt{tennis\_ball}} & margin (mm) $\uparrow$ & 16.0 (15.2, 16.0) & 1.0 (1.0, 1.0) \\
 & near $\downarrow$ & 0\% [0, 39] & 100\% [61, 100] \\
\midrule
\multirow{2}{*}{\texttt{foam\_brick}} & margin (mm) $\uparrow$ & 11.5 (10.2, 12.0) & 1.0 (1.0, 1.0) \\
 & near $\downarrow$ & 0\% [0, 39] & 100\% [61, 100] \\
\midrule
\multirow{2}{*}{\textbf{all}} & margin (mm) $\uparrow$ & \textbf{13.5 (12.0, 16.0)} & 1.0 (1.0, 1.0) \\
 & near $\downarrow$ & \textbf{0\% [0, 11]} & 87\% [70, 95] \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Edge-seeking}

Li et al.\ identify edge-seeking---contacts migrating to surface discontinuities,
where the friction cone falls away on both sides---as the dominant failure mode of
both their method and their baseline, and leave it to future work without proposing a
metric. We supply one: for each contact, the geodesic distance along the surface to
the nearest point where the normal turns more than $30^\circ$, searched along 16
tangent directions with reprojection at each step. It queries the mesh directly and so
is independent of contact parameterization---necessary here, as the baseline's
contacts are forward-kinematics outputs carrying no local surface model.

Table~\ref{tab:edge} reports the result: our contacts sit a median of
\SI{13.5}{\milli\meter} from the nearest edge against \SI{1.0}{\milli\meter}, and
none of our 30 fall within \SI{2}{\milli\meter} of an edge against 26 of 30.
\SI{1.0}{\milli\meter} is the search step, so those contacts are on the edge, not
near it. \texttt{017\_orange} is the control---a near-sphere presents no edge to
seek, and is the one object where margins are comparable. Edge proximity predicts
outcome: grasps that held sit a median of \SI{12.0}{\milli\meter} from an edge
($n=18$) against \SI{1.0}{\milli\meter} for those that failed ($n=12$),
$r = +0.574$. This is the mechanism behind~\cite{li2023frogger}'s ``yields unstable
grasps in practice,'' measured.

\subsubsection{$\bar{\ell}^*$ is a weak predictor of success}

Li et al.\ report $\bar{\ell}^*$ separating successes from failures only weakly. We
reproduce this (pooled medians $0.87$ against $0.52$, $r = +0.317$) and sharpen it:
the correlation is $r = +0.474$ for the baseline but $r = +0.170$ for ours, whose
solutions are near-saturated ($0.88$ against $0.71$). A metric optimized by one
method is not a neutral instrument for scoring another, and the edge margin of
Table~\ref{tab:edge} predicts outcomes better than $\bar{\ell}^*$ does for either.

\subsubsection{Which mechanisms account for the difference}

Three components bear on the gap, each measured on \texttt{036\_wood\_block} unless
noted. \textbf{Curvature is fitted to local mesh vertices rather than the SDF
Hessian}, whose second derivative is global: mid-face where $\kappa = 0$, the Hessian
reports up to \SI{9.05}{\per\meter} sourced from a corner \SI{50}{\milli\meter} away,
and fake curvature of $-12.33$ collapsed a trust region to \SI{4.6}{\milli\meter} on
a face with \SI{104}{\milli\meter} of usable surface. \textbf{Each contact carries
explicit trust-region bounds}, sized per axis by marching outward until the
paraboloid surrogate departs from the true signed distance by a tolerance, with
bounds set by measured divergence distinguished from those merely reaching the search
cap; this is where the margins of Table~\ref{tab:edge} originate, and a uniform
corner shrink was needed to bring patch error from \SI{8.78}{\milli\meter} under its
\SI{4}{\milli\meter} tolerance. \textbf{The commanded internal force is a
task-specific certificate re-solved on achieved geometry}---an acceleration budget
expanded into a force and torque box with gravity re-datumed about the contact
centroid, infeasibility returning no grasp rather than a fabricated force---and its
allocation is cone-constrained rather than sign-anchored, which on an asymmetric
three-contact grasp gives normal forces of $[1.000, 1.000, 1.000]\,\si{\newton}$
where sign-anchoring gives $[1.213, 0.047, 0.325]\,\si{\newton}$.

\subsubsection{Limitations}

The comparison is asymmetric in ways that favor us. Our planner is tuned on this
scene; the baseline is a reimplementation, and one defect---contacts reported at the
fingertip site rather than the constrained pad point, displacing each by
approximately \SI{10}{\milli\meter}---was found and corrected during this study,
raising its success rate from $7\%$ to $35\%$. We cannot exclude further such
defects, and the $25\%$ of baseline trials placing unreachable contacts is where we
would look first. Friction and robustness floor differ from the original work, and
plan-time quantities are measured over 30 of the 200 trials.
